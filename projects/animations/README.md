## Animation related to NLP:


In order to run this animations run 
```bash
docker run --rm -it -v "/path/to/this/directory:/manim" manimcommunity/manim manim -qm text_embedding.py TextEmbedding 
```
